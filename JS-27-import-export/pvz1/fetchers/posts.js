const postEndpoint = "https://jsonplaceholder.typicode.com/posts";

export async function getPosts() {
  try {
    const resposnse = await fetch(postEndpoint);
    if (condition) {
    } else {
    }
  } catch (error) {}
}
